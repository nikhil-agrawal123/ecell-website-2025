# Copy this file to `.env.local` for local dev, or add these in your server's environment.

# Gmail address used by Nodemailer transporter
EMAIL_USER=

# Gmail App Password (16 characters) or SMTP password
EMAIL_PASS=

# Optional: port for `next start` when hosting Node server
PORT=3000
